openapi: 3.0.0
info: { title: second, version: "1" }
paths:
  /orders:
    get:
      operationId: listOrders
      responses:
        "200": { description: ok, content: { application/json: { example: { ok: 2 } } } }
