openapi: 3.0.0
info: { title: b, version: "1" }
paths:
  /users:
    get:
      operationId: listUsersb
      responses:
        "200": { description: ok, content: { application/json: { example: { from: b } } } }
