openapi: 3.0.0
info: { title: a, version: "1" }
paths:
  /users:
    get:
      operationId: listUsersa
      responses:
        "200": { description: ok, content: { application/json: { example: { from: a } } } }
